#pragma once
// BridgeHandler
// Handles the approach-marker detection, incline climb, and
// line-following across the bridge span + post-entry corridor
// until standard maze floor (black floor / white walls) resumes.

class BridgeHandler {
public:
    // TODO: detectApproachMarker(), followLineAcrossBridge(), hasReachedSectionB()
};
