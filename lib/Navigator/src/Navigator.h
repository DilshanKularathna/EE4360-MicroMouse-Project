#pragma once
// Navigator
// Movement primitives (moveForwardOneCell, turnLeft90, turnRight90, ...)
// plus DFS/exploration logic and shortest-path execution,
// built on top of MotorDriver + Encoder + UltrasonicSensor + MazeMapper.

class Navigator {
public:
    // TODO: exploreStep(), moveForwardOneCell(), turnLeft90(), turnRight90(),
    //       runShortestPath()
};
