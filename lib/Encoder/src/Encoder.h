#pragma once
// Encoder
// Interrupt-driven quadrature pulse counting for one wheel.
// Used by Navigator for distance/turn odometry.

class Encoder {
public:
    // TODO: begin(pinA, pinB), getCount(), reset(), ISR handler
};
