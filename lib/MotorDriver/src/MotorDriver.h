#pragma once
// MotorDriver
// Wraps the H-bridge motor driver module. Provides per-side speed/direction
// control so higher-level code (Navigator, PIDController) never touches pins.

class MotorDriver {
public:
    // TODO: constructor(pins), setLeftSpeed(), setRightSpeed(), stop()
};
