#pragma once
// LineSensorArray
// 8-channel IR sensor array.
// Responsibilities:
//  - compute line-following error (for PIDController)
//  - detect floor patterns: black floor, white start/finish tile,
//    approach marker (white square with black square)

class LineSensorArray {
public:
    // TODO: begin(pins[8]), readRaw(), getLineError(), detectFloorPattern()
};
